# Environment Variables for Todo Backend
# Copy this file to .env and fill in the values

# =============================================================================
# Database Configuration
# =============================================================================
# Supports both SQLite (development) and PostgreSQL (production)

# OPTION A: SQLite for Development (no Docker required)
# Uses aiosqlite driver for async support
DATABASE_URL=sqlite+aiosqlite:///./todo_dev.db

# OPTION B: PostgreSQL for Production (asyncpg format)
# Example: postgresql+asyncpg://user:password@localhost:5432/todo_db
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# OPTION C: Neon Serverless PostgreSQL (production)
# DATABASE_URL=postgresql+asyncpg://user:password@ep-cool-name.us-east-2.aws.neon.tech/neondb?sslmode=require

# JWT Secret Key (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32

# JWT Algorithm (default: HS256)
ALGORITHM=HS256

# Access Token Expiration in Minutes (default: 1440 = 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS Origins (comma-separated list, or JSON array)
# Example: ["http://localhost:3000", "https://yourdomain.com"]
CORS_ORIGINS=["http://localhost:3000"]
