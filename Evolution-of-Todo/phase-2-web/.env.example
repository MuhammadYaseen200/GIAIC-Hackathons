# =============================================================================
# Evolution of Todo - Phase 2 Environment Configuration
# =============================================================================
# Copy this file to .env and update values as needed.
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Docker Compose)
# -----------------------------------------------------------------------------
POSTGRES_DB=todo_db
POSTGRES_USER=todo_user
POSTGRES_PASSWORD=dev_password_change_me
POSTGRES_PORT=5432

# Database URL for backend (local development)
# For Docker: postgresql+asyncpg://todo_user:dev_password_change_me@localhost:5432/todo_db
# For Neon: postgresql+asyncpg://user:pass@host/db?sslmode=require
DATABASE_URL=postgresql+asyncpg://todo_user:dev_password_change_me@localhost:5432/todo_db

# -----------------------------------------------------------------------------
# Backend Configuration
# -----------------------------------------------------------------------------
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# JWT Configuration
# IMPORTANT: Generate a secure secret for production!
# Example: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=replace_with_32_char_secret_for_production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_SECONDS=86400

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
# Public API URL (exposed to browser for client-side fetches if any)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend URL for server-side API calls (Next.js Server Actions)
BACKEND_URL=http://localhost:8000
